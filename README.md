# 🤖 ChatVLM - Vision Language Models Interface

> Универсальный интерфейс для работы с Vision-Language моделями с поддержкой OCR и анализа изображений.

[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/downloads/)
[![Streamlit](https://img.shields.io/badge/Streamlit-1.28+-red.svg)](https://streamlit.io)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

## ✨ Возможности

- 🖼️ **Анализ изображений** — распознавание текста, описание содержимого
- 📊 **OCR с разметкой** — извлечение текста с координатами (BBOX)
- 💬 **Чат-интерфейс** — мультимодальный диалог с контекстом
- ⚡ **vLLM интеграция** — высокопроизводительный инференс
- 🔄 **Локальные модели** — работа без интернета

## 🚀 Быстрый старт

### Требования

- Python 3.10+
- NVIDIA GPU с CUDA 12.1+
- Docker (для vLLM режима)
- 16GB+ GPU VRAM (рекомендуется 24GB+)

### Установка

```bash
# Клонирование
git clone https://github.com/OlegKarenkikh/chatvlmllm.git
cd chatvlmllm

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# Установка зависимостей
pip install -r requirements.txt
```

### Запуск

```bash
# Запуск Streamlit приложения
streamlit run app.py

# Или с Docker Compose
docker-compose up -d
```

## 📁 Структура проекта

```
chatvlmllm/
├── app.py                      # Точка входа Streamlit
├── config/                     # Конфигурация
│   ├── __init__.py
│   └── settings.py             # Централизованные настройки
├── core/                       # Бизнес-логика
│   ├── __init__.py
│   └── error_handler.py        # Обработка ошибок
├── services/                   # Сервисы
│   ├── __init__.py
│   └── model_service.py        # Сервис моделей
├── ui/                         # UI компоненты
│   ├── __init__.py
│   └── components/
│       ├── message_renderer.py # Рендеринг сообщений
│       └── sidebar.py          # Боковая панель
├── utils/                      # Утилиты
│   ├── __init__.py
│   ├── converters.py           # Конвертеры данных
│   └── text_cleaner.py         # Очистка текста
├── models/                     # Директория для моделей
├── model_loader.py             # Загрузчик моделей
├── vllm_streamlit_adapter.py   # vLLM адаптер
├── single_container_manager.py # Менеджер контейнеров
└── requirements.txt
```

## 🎯 Использование

### Режим OCR

1. Выберите модель с поддержкой OCR (например, InternVL2 или DOTS)
2. Загрузите изображение
3. Выберите тип OCR: простой текст или с разметкой (BBOX)
4. Получите результат в удобном формате

### Режим чата

1. Выберите VLM модель
2. Загрузите изображение (опционально)
3. Введите вопрос или команду
4. Получите ответ с учётом изображения

## ⚙️ Конфигурация

### Настройки в `.env`

```env
# Пути к моделям
MODELS_PATH=/path/to/models

# vLLM настройки
VLLM_GPU_MEMORY_UTILIZATION=0.9
VLLM_MAX_MODEL_LEN=32768

# Streamlit
STREAMLIT_SERVER_PORT=8501
```

### Поддерживаемые модели

| Модель | Размер | vLLM | OCR | Чат |
|--------|--------|------|-----|-----|
| Qwen2-VL-7B | 7B | ✅ | ✅ | ✅ |
| InternVL2-8B | 8B | ✅ | ✅ | ✅ |
| DOTS-1B | 1B | ✅ | ✅ | ❌ |
| Llama-3.2-Vision | 11B | ✅ | ✅ | ✅ |

## 🛠️ Разработка

### Архитектура

Проект следует принципам чистой архитектуры:

- **config/** — конфигурация и константы
- **core/** — бизнес-логика (не зависит от UI)
- **services/** — сервисы для работы с моделями
- **ui/** — Streamlit UI компоненты
- **utils/** — вспомогательные утилиты

### Запуск тестов

```bash
pytest tests/ -v
```

## 📄 Лицензия

MIT License — см. [LICENSE](LICENSE)

## 🤝 Контрибуция

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing`)
5. Откройте Pull Request
