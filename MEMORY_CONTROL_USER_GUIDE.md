# 🧠 Руководство по контролю памяти

## 🎯 Что это дает

Система контроля памяти автоматически управляет переключением между режимами vLLM и Transformers, контролирует выгрузку моделей и предотвращает ошибки нехватки памяти.

## 🚀 Как использовать

### 1. Откройте управление памятью
В боковой панели приложения найдите раздел:
```
🔧 Управление режимами и памятью
```

### 2. Мониторинг памяти
Система показывает:
- **GPU память (всего)** - общий объем
- **Используется** - текущее использование
- **Доступно** - свободная память
- **Утилизация** - процент использования

### 3. Переключение режимов

#### Автоматическое переключение:
1. Выберите **режим выполнения** (vLLM или Transformers)
2. Выберите **модель** из кешированных
3. Нажмите **"🔄 Применить изменения"**

Система автоматически:
- Выгрузит текущие модели
- Очистит GPU память
- Проверит доступность ресурсов
- Переключит режим
- Подготовит новую модель

#### Ручное управление:
- **"🧹 Очистить память"** - очистка GPU кеша
- **"🚨 Экстренная очистка"** - полная очистка всех ресурсов

### 4. Управление моделями

#### Transformers модели:
- Список загруженных моделей
- Кнопка **"🗑️"** для выгрузки каждой модели

#### vLLM контейнеры:
- Список запущенных контейнеров
- Кнопка **"🛑"** для остановки каждого контейнера

## 💡 Рекомендации системы

Система автоматически анализирует доступную память и дает рекомендации:

### При 8+ GB свободной памяти:
- ✅ **vLLM режим**
- ✅ FP16 точность
- ✅ 2048 токенов
- ✅ Батч размер 4

### При 6-8 GB свободной памяти:
- ⚠️ **vLLM режим** (ограниченный)
- ⚠️ FP16 точность
- ⚠️ 1024 токена
- ⚠️ Батч размер 2

### При 4-6 GB свободной памяти:
- 🔧 **Transformers режим**
- 🔧 FP16 точность
- 🔧 1024 токена
- 🔧 Батч размер 1

### При менее 4 GB:
- ⚠️ **Transformers режим**
- ⚠️ INT8 квантизация
- ⚠️ 512 токенов
- ⚠️ Батч размер 1

## 🚨 Индикаторы состояния

### Память:
- 🟢 **Зеленый** - достаточно памяти (4+ GB)
- 🟡 **Желтый** - мало памяти (2-4 GB)
- 🔴 **Красный** - критически мало памяти (<2 GB)

### Операции:
- ✅ **Успех** - операция выполнена
- ⚠️ **Предупреждение** - есть ограничения
- ❌ **Ошибка** - операция не удалась

## 🔧 Решение проблем

### Проблема: "Недостаточно памяти"
**Решение:**
1. Нажмите **"🧹 Очистить память"**
2. Если не помогло - **"🚨 Экстренная очистка"**
3. Выберите более легкую модель
4. Переключитесь на Transformers режим

### Проблема: "Модель не загружается"
**Решение:**
1. Проверьте, что модель есть в кеше
2. Убедитесь, что достаточно памяти
3. Попробуйте экстренную очистку
4. Перезапустите приложение

### Проблема: "Контейнер не останавливается"
**Решение:**
1. Используйте **"🚨 Экстренная очистка"**
2. Проверьте Docker статус вручную
3. Перезапустите Docker службу

## 📊 Мониторинг

### Кешированные модели:
Показывает список моделей, доступных для быстрой загрузки

### Загруженные модели:
- **Transformers** - модели в памяти Python
- **vLLM** - контейнеры Docker

### Статус памяти:
Обновляется в реальном времени при операциях

## 🎯 Лучшие практики

### 1. Планирование памяти:
- Проверяйте статус перед загрузкой
- Следуйте рекомендациям системы
- Выгружайте неиспользуемые модели

### 2. Переключение режимов:
- Используйте автоматическое переключение
- Дождитесь завершения операций
- Проверяйте результат

### 3. Экстренные ситуации:
- При зависании - экстренная очистка
- При ошибках - перезапуск приложения
- При критической нехватке памяти - перезагрузка системы

## 🧪 Тестирование

Для тестирования системы запустите:
```bash
streamlit run test_memory_controller.py
```

Тест покажет:
- Текущий статус памяти
- Работу переключения режимов
- Функции очистки памяти
- Рекомендации системы

---

**🎉 Система контроля памяти делает работу с моделями безопасной и эффективной!**