# 🚀 РУКОВОДСТВО ПО ВЫБОРУ QWEN3-VL В vLLM РЕЖИМЕ

**Дата:** 25 января 2026  
**Статус:** ✅ **НАСТРОЕНО И ГОТОВО К ИСПОЛЬЗОВАНИЮ**  

---

## 🎯 КАК ВЫБРАТЬ QWEN3-VL В ПРИЛОЖЕНИИ

### 1️⃣ **Откройте приложение**
- URL: http://localhost:8504

### 2️⃣ **Выберите vLLM режим**
В боковой панели найдите:
```
🚀 Режим выполнения
[vLLM (Рекомендуется)] ← Выберите это
```

### 3️⃣ **Выберите модель Qwen3-VL**
После выбора vLLM режима появится список моделей:
```
Выберите модель (vLLM)
[Qwen3-VL-2B-Instruct] ← Выберите это
```

### 4️⃣ **Проверьте статус модели**
Вы должны увидеть:
```
🚀 vLLM: Qwen3-VL-2B-Instruct
🟢 vLLM режим - высокая производительность
🎯 Max Tokens: 4096
📏 Модель: Qwen/Qwen3-VL-2B-Instruct
⚡ Статус: Готов к использованию
```

---

## 🔧 ТЕКУЩИЙ СТАТУС НАСТРОЙКИ

### ✅ **Что уже настроено:**

1. **Docker контейнер запущен:**
   - Контейнер: `qwen-qwen3-vl-2b-instruct-vllm`
   - Порт: `8004`
   - Статус: Загружается (первый запуск)

2. **Конфигурация готова:**
   - `config.yaml` содержит настройки Qwen3-VL
   - `docker-compose-vllm.yml` настроен
   - `vllm_streamlit_adapter.py` поддерживает Qwen3

3. **Приложение готово:**
   - Интерфейс поддерживает выбор Qwen3
   - Адаптер настроен на порт 8004

### ⏳ **Что происходит сейчас:**

Модель **Qwen3-VL 2B загружается** в контейнер. Это может занять **5-10 минут** при первом запуске, так как:
- Скачиваются веса модели (~4GB)
- Инициализируется CUDA
- Настраивается vLLM сервер

---

## 🧪 КАК ПРОВЕРИТЬ ГОТОВНОСТЬ

### **Способ 1: Через Docker**
```bash
docker logs qwen-qwen3-vl-2b-instruct-vllm --tail 10
```

**Ищите сообщение:**
```
INFO: Uvicorn running on http://0.0.0.0:8000
```

### **Способ 2: Через браузер**
Откройте: http://localhost:8004/health

**Должно показать:**
```json
{"status": "ok"}
```

### **Способ 3: В приложении**
1. Откройте http://localhost:8504
2. Выберите "vLLM (Рекомендуется)"
3. Если Qwen3-VL появился в списке - готово!

---

## 🎨 ПРЕИМУЩЕСТВА QWEN3-VL

### **🚀 Производительность:**
- **Быстрая генерация** через vLLM
- **Оптимизированная память** (6GB VRAM)
- **Высокая пропускная способность**

### **🎯 Качество:**
- **Улучшенная точность** по сравнению с Qwen2-VL
- **Лучшее понимание** сложных документов
- **Поддержка длинных контекстов** (4096 токенов)

### **🔧 Функции:**
- **OCR документов** (паспорта, счета, чеки)
- **Анализ изображений** с детальным описанием
- **Чат с изображениями** - задавайте вопросы
- **BBOX детекция** - находит элементы на изображении

---

## 🛠️ УСТРАНЕНИЕ ПРОБЛЕМ

### **❌ Модель не появляется в списке**

**Причина:** Контейнер еще загружается

**Решение:**
1. Подождите 5-10 минут
2. Обновите страницу приложения (F5)
3. Проверьте логи: `docker logs qwen-qwen3-vl-2b-instruct-vllm`

### **❌ Ошибка "Нет доступных моделей в vLLM"**

**Причина:** vLLM сервер не готов

**Решение:**
```bash
# Перезапустите контейнер
docker-compose -f docker-compose-vllm.yml restart qwen3-vl-2b

# Проверьте статус
docker ps --filter "name=qwen3"
```

### **❌ Ошибка памяти GPU**

**Причина:** Недостаточно VRAM

**Решение:**
1. Закройте другие GPU приложения
2. Используйте только одну модель одновременно
3. Перезапустите Docker: `docker-compose -f docker-compose-vllm.yml down && docker-compose -f docker-compose-vllm.yml up -d qwen3-vl-2b`

---

## 📋 БЫСТРЫЙ ЧЕКЛИСТ

- [ ] Контейнер запущен: `docker ps | grep qwen3`
- [ ] Health check OK: `curl http://localhost:8004/health`
- [ ] Приложение открыто: http://localhost:8504
- [ ] Выбран vLLM режим
- [ ] Выбрана модель Qwen3-VL-2B-Instruct
- [ ] Статус "Готов к использованию"

---

## 🎊 ГОТОВО К ИСПОЛЬЗОВАНИЮ!

После завершения загрузки модели вы сможете:

1. **📄 OCR режим:** Загружайте документы для распознавания текста
2. **💬 Чат режим:** Общайтесь с моделью и задавайте вопросы об изображениях
3. **🔍 BBOX анализ:** Включайте детекцию элементов на документах
4. **⚙️ Настройки:** Регулируйте температуру и количество токенов

**Модель Qwen3-VL готова обрабатывать ваши документы с высокой скоростью и точностью!** 🚀