# Руководство по участию в проекте

Спасибо за ваш интерес к проекту ChatVLMLLM! Мы ценим любой вклад в развитие проекта.

## 📋 Содержание

- [Кодекс поведения](#кодекс-поведения)
- [Как внести вклад](#как-внести-вклад)
- [Процесс разработки](#процесс-разработки)
- [Стандарты кода](#стандарты-кода)
- [Тестирование](#тестирование)
- [Документация](#документация)
- [Коммиты и Pull Requests](#коммиты-и-pull-requests)

## 🤝 Кодекс поведения

Этот проект придерживается следующих принципов:

- **Уважение**: Относитесь ко всем участникам с уважением
- **Открытость**: Будьте открыты к конструктивной критике
- **Сотрудничество**: Работайте вместе для достижения общих целей
- **Профессионализм**: Поддерживайте профессиональную атмосферу

## 🚀 Как внести вклад

### Типы вкладов

#### 🐛 Сообщения об ошибках

При обнаружении ошибки создайте issue с:
- Описанием проблемы
- Шагами для воспроизведения
- Ожидаемым и фактическим поведением
- Версией Python, CUDA, GPU
- Логами ошибок

#### ✨ Предложения функций

Для новых функций создайте issue с:
- Описанием функции
- Обоснованием необходимости
- Примерами использования
- Возможными альтернативами

#### 📝 Улучшение документации

Документация всегда может быть лучше:
- Исправление опечаток
- Улучшение примеров
- Добавление диаграмм
- Перевод на другие языки

#### 🔧 Исправления кода

Исправления ошибок и улучшения кода приветствуются:
- Исправление багов
- Оптимизация производительности
- Рефакторинг кода
- Добавление тестов

## 🔄 Процесс разработки

### 1. Fork репозитория

```bash
# Форкните репозиторий через GitHub UI
# Затем клонируйте ваш форк
git clone https://github.com/YOUR_USERNAME/chatvlmllm.git
cd chatvlmllm
```

### 2. Создайте ветку

```bash
# Создайте ветку от main
git checkout -b feature/amazing-feature

# Или для исправления
git checkout -b fix/bug-description
```

Названия веток:
- `feature/` - новые функции
- `fix/` - исправление ошибок
- `docs/` - изменения документации
- `refactor/` - рефакторинг кода
- `test/` - добавление тестов

### 3. Настройте окружение

```bash
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# Установите зависимости для разработки
pip install -r requirements.txt
pip install -r requirements-dev.txt  # если есть

# Установите pre-commit hooks (если настроены)
pre-commit install
```

### 4. Внесите изменения

Следуйте [стандартам кода](#стандарты-кода) при разработке.

### 5. Протестируйте изменения

```bash
# Запустите тесты
pytest tests/ -v

# Проверьте покрытие
pytest --cov=. tests/

# Проверьте стиль кода (если настроено)
flake8 .
black --check .
```

### 6. Зафиксируйте изменения

Следуйте [соглашению о коммитах](#соглашение-о-коммитах).

```bash
git add .
git commit -m "feat: add amazing feature"
```

### 7. Отправьте изменения

```bash
git push origin feature/amazing-feature
```

### 8. Создайте Pull Request

1. Перейдите на GitHub
2. Нажмите "New Pull Request"
3. Заполните шаблон PR
4. Дождитесь ревью

## 📏 Стандарты кода

### Python стиль

Мы следуем [PEP 8](https://pep8.org/) с некоторыми исключениями:

```python
# ✅ Хорошо
class ModelLoader:
    """Загрузчик моделей с поддержкой кеширования."""
    
    def load_model(self, model_name: str) -> Model:
        """Загружает модель по имени.
        
        Args:
            model_name: Название модели из config.yaml
            
        Returns:
            Загруженная модель
            
        Raises:
            ModelNotFoundError: Если модель не найдена
        """
        pass

# ❌ Плохо
class modelLoader:  # camelCase
    def LoadModel(self, n):  # нет документации, неясный параметр
        pass
```

### Форматирование

- **Отступы**: 4 пробела (не табы)
- **Длина строки**: максимум 100 символов
- **Импорты**: группируйте и сортируйте
  ```python
  # Стандартная библиотека
  import os
  import sys
  
  # Сторонние библиотеки
  import torch
  import streamlit as st
  
  # Локальные импорты
  from models import ModelLoader
  from utils import clean_text
  ```

### Документация кода

```python
def process_image(image: Image, model: str, max_tokens: int = 1024) -> str:
    """Обрабатывает изображение с помощью VLM модели.
    
    Эта функция принимает изображение и использует указанную модель
    для извлечения текста или генерации описания.
    
    Args:
        image: PIL изображение для обработки
        model: Название модели из config.yaml
        max_tokens: Максимальное количество токенов для генерации
        
    Returns:
        Извлеченный текст или описание изображения
        
    Raises:
        ValueError: Если изображение невалидно
        ModelError: Если модель не загружена
        
    Example:
        >>> from PIL import Image
        >>> image = Image.open('document.jpg')
        >>> text = process_image(image, 'qwen3_vl_2b')
        >>> print(text)
        'Распознанный текст из документа...'
    """
    pass
```

### Type Hints

Используйте аннотации типов:

```python
from typing import List, Dict, Optional, Union
from pathlib import Path

def load_config(path: Path) -> Dict[str, any]:
    """Загружает конфигурацию из файла."""
    pass

def process_batch(images: List[Image], 
                  model: Optional[str] = None) -> List[str]:
    """Обрабатывает пакет изображений."""
    pass
```

## 🧪 Тестирование

### Написание тестов

Все новые функции должны иметь тесты:

```python
# tests/test_model_loader.py
import pytest
from models import ModelLoader

def test_load_model_success():
    """Тест успешной загрузки модели."""
    loader = ModelLoader()
    model = loader.load_model('qwen3_vl_2b')
    assert model is not None
    
def test_load_model_invalid_name():
    """Тест загрузки несуществующей модели."""
    loader = ModelLoader()
    with pytest.raises(ModelNotFoundError):
        loader.load_model('invalid_model')

@pytest.mark.gpu
def test_model_inference():
    """Тест инференса модели (требует GPU)."""
    # Тесты, требующие GPU
    pass
```

### Запуск тестов

```bash
# Все тесты
pytest tests/ -v

# Конкретный файл
pytest tests/test_model_loader.py -v

# С покрытием
pytest --cov=models tests/test_model_loader.py

# Пропустить GPU тесты
pytest -m "not gpu" tests/
```

## 📚 Документация

### Структура документации

```
docs/
├── README.md           # Обзор документации
├── quickstart.md       # Быстрый старт
├── models.md           # Описание моделей
├── architecture.md     # Архитектура системы
├── api_guide.md        # Руководство по API
└── gpu_requirements.md # Требования к GPU
```

### Обновление документации

При изменении кода обновляйте:
- Docstrings функций/классов
- README.md (если меняется API)
- Соответствующие разделы в `docs/`
- Примеры кода

## 📝 Коммиты и Pull Requests

### Соглашение о коммитах

Мы используем [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Types:**
- `feat`: Новая функция
- `fix`: Исправление ошибки
- `docs`: Изменения документации
- `style`: Форматирование, отсутствующие точки с запятой и т.д.
- `refactor`: Рефакторинг кода
- `test`: Добавление тестов
- `chore`: Обновление зависимостей, конфигурации и т.д.

**Примеры:**

```bash
# Новая функция
git commit -m "feat(models): add support for Qwen3-VL 8B model"

# Исправление
git commit -m "fix(ui): resolve BBOX visualization crash on large images"

# Документация
git commit -m "docs: update installation instructions for CUDA 12.1"

# Рефакторинг
git commit -m "refactor(app): extract message rendering to separate module"
```

### Шаблон Pull Request

```markdown
## Описание
Краткое описание изменений

## Тип изменения
- [ ] Bug fix (исправление, не ломающее существующий функционал)
- [ ] New feature (новая функция, не ломающая существующий функционал)
- [ ] Breaking change (изменение, которое может сломать существующий функционал)
- [ ] Documentation update (изменения только в документации)

## Как протестировано
Описание тестов и их результатов

## Checklist
- [ ] Код следует стандартам проекта
- [ ] Проведено самостоятельное ревью кода
- [ ] Добавлены комментарии в сложных местах
- [ ] Обновлена документация
- [ ] Изменения не генерируют новых предупреждений
- [ ] Добавлены тесты
- [ ] Все новые и существующие тесты проходят

## Связанные issues
Закрывает #123
```

## 🔍 Процесс ревью

### Что проверяют ревьюеры

1. **Функциональность**: Работает ли код как задумано?
2. **Тесты**: Покрыты ли изменения тестами?
3. **Стиль**: Соответствует ли код стандартам?
4. **Документация**: Обновлена ли документация?
5. **Производительность**: Нет ли проблем с производительностью?

### Как реагировать на комментарии

```bash
# Внесите изменения по комментариям
git add .
git commit -m "fix: address review comments"
git push origin feature/amazing-feature
```

## 💡 Советы для новичков

### С чего начать

1. Посмотрите issues с меткой `good first issue`
2. Прочитайте существующий код для понимания структуры
3. Начните с малого (исправление опечаток, улучшение документации)
4. Задавайте вопросы, если что-то непонятно

### Полезные ресурсы

- [Документация проекта](docs/)
- [Python Style Guide](https://pep8.org/)
- [Git Workflow](https://guides.github.com/introduction/flow/)
- [Conventional Commits](https://www.conventionalcommits.org/)

## 📞 Контакты

- **Issues**: https://github.com/OlegKarenkikh/chatvlmllm/issues
- **Discussions**: https://github.com/OlegKarenkikh/chatvlmllm/discussions
- **Email**: karenkikh@example.com

## 📄 Лицензия

Внося вклад в проект, вы соглашаетесь, что ваш код будет лицензирован под [MIT License](LICENSE).

---

**Спасибо за ваш вклад! 🎉**

Вместе мы делаем ChatVLMLLM лучше!