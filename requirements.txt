# Core ML
# Note: torch and torchvision are installed separately in Dockerfile
# to handle CPU/CUDA versions properly
transformers>=4.40.0
accelerate>=0.27.0

# VLM specific
qwen-vl-utils>=0.0.8
# flash-attn requires CUDA - only in requirements-cuda.txt
# bitsandbytes requires CUDA - only in requirements-cuda.txt
einops>=0.7.0
tiktoken>=0.6.0

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# UI
streamlit>=1.28.0

# Utils
Pillow>=10.0.0
PyYAML>=6.0
requests>=2.31.0
numpy>=1.24.0
pandas>=2.0.0
opencv-python-headless>=4.8.0
python-magic>=0.4.27
