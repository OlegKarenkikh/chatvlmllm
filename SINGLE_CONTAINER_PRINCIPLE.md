# 🎯 ПРИНЦИП ОДНОГО АКТИВНОГО КОНТЕЙНЕРА

**Дата:** 25 января 2026  
**Статус:** ✅ **РЕАЛИЗОВАН И АКТИВЕН**  
**Принцип:** Только один vLLM контейнер активен одновременно  

---

## 🔍 ПРОБЛЕМА, КОТОРУЮ РЕШАЕМ

### ❌ **Старая система (множественные контейнеры):**
- 🔥 **Нехватка GPU памяти** при запуске нескольких моделей
- 💥 **Крэши контейнеров** из-за превышения лимитов VRAM
- 🐌 **Медленная работа** из-за конкуренции за ресурсы
- 🔄 **Нестабильность** системы при переключении моделей

### ✅ **Новая система (один активный контейнер):**
- 💾 **Гарантированная память** для активной модели
- ⚡ **Максимальная производительность** без конкуренции
- 🛡️ **Стабильная работа** системы
- 🎯 **Предсказуемое поведение** при переключении

---

## 🎯 ПРИНЦИП РАБОТЫ

### **Основное правило:**
> **В любой момент времени активен только ОДИН vLLM контейнер**

### **Алгоритм переключения:**
1. **Пользователь выбирает модель** в интерфейсе
2. **Система останавливает** все активные контейнеры
3. **Освобождается GPU память** (пауза 3 секунды)
4. **Запускается целевой контейнер** с выбранной моделью
5. **Ожидание готовности** модели (health check + API check)
6. **Модель готова** к использованию

---

## 🏗️ АРХИТЕКТУРА СИСТЕМЫ

### **Компоненты:**

#### 1️⃣ **SingleContainerManager**
- **Файл:** `single_container_manager.py`
- **Функция:** Управление жизненным циклом контейнеров
- **Принцип:** Строгое соблюдение правила "один активный"

#### 2️⃣ **VLLMStreamlitAdapter** (обновлен)
- **Файл:** `vllm_streamlit_adapter.py`
- **Функция:** Интеграция с Streamlit интерфейсом
- **Изменение:** Использует SingleContainerManager вместо VLLMMemoryManager

#### 3️⃣ **Docker Compose конфигурация**
- **Файл:** `docker-compose-vllm.yml`
- **Функция:** Определение всех доступных моделей
- **Особенность:** Модели запускаются по требованию

---

## 📋 ДОСТУПНЫЕ МОДЕЛИ

| Модель | Контейнер | Порт | Память | Время запуска |
|--------|-----------|------|--------|---------------|
| **dots.ocr** | `dots-ocr-fixed` | 8000 | 4.5 ГБ | ~60 сек |
| **Qwen3-VL 2B** | `qwen-qwen3-vl-2b-instruct-vllm` | 8004 | 6.5 ГБ | ~120 сек |
| **Qwen2-VL 2B** | `qwen-qwen2-vl-2b-instruct-vllm` | 8001 | 6.0 ГБ | ~100 сек |
| **Phi-3.5 Vision** | `microsoft-phi-3-5-vision-instruct-vllm` | 8002 | 8.0 ГБ | ~150 сек |

---

## 🎮 КАК ИСПОЛЬЗОВАТЬ

### **В Streamlit приложении:**

1. **Откройте приложение:** http://localhost:8504

2. **Выберите vLLM режим:**
   ```
   🚀 Режим выполнения
   [vLLM (Рекомендуется)] ← Выберите
   ```

3. **Выберите модель:**
   ```
   Выберите модель (vLLM)
   [Qwen3-VL-2B-Instruct] ← Выберите нужную
   ```

4. **Система автоматически:**
   - Остановит все другие контейнеры
   - Запустит выбранную модель
   - Покажет прогресс загрузки
   - Уведомит о готовности

### **Через SingleContainerManager:**

```python
from single_container_manager import SingleContainerManager

# Инициализация
manager = SingleContainerManager()

# Переключение на Qwen3
success, message = manager.start_single_container("qwen3-vl-2b")

# Проверка статуса
status = manager.get_system_status()
print(f"Активная модель: {status['active_model_name']}")
```

---

## 💡 ПРЕИМУЩЕСТВА НОВОГО ПРИНЦИПА

### **🚀 Производительность:**
- **100% GPU памяти** доступно активной модели
- **Максимальная скорость** генерации
- **Нет конкуренции** за ресурсы

### **🛡️ Стабильность:**
- **Исключены крэши** из-за нехватки памяти
- **Предсказуемое поведение** системы
- **Надежное переключение** между моделями

### **💾 Эффективность памяти:**
- **Экономия 70-80%** GPU памяти
- **Возможность использования** больших моделей
- **Оптимальное использование** ресурсов

### **🎯 Простота использования:**
- **Автоматическое управление** контейнерами
- **Прозрачное переключение** для пользователя
- **Понятные статусы** и уведомления

---

## 🔧 ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ

### **Состояния контейнера:**
- **⚪ Остановлен** - контейнер не запущен
- **🟡 Запускается** - контейнер в процессе загрузки
- **🟢 Активен** - контейнер готов к работе
- **❌ Ошибка** - проблемы с запуском

### **Проверки готовности:**
1. **Container Status** - статус Docker контейнера
2. **Health Check** - `/health` endpoint
3. **API Check** - `/v1/models` endpoint
4. **Model Availability** - наличие модели в API

### **Управление памятью:**
```python
# Конфигурация моделей с потреблением памяти
models_config = {
    "dots.ocr": {"memory_gb": 4.5},
    "qwen3-vl-2b": {"memory_gb": 6.5},
    "qwen2-vl-2b": {"memory_gb": 6.0},
    "phi35-vision": {"memory_gb": 8.0}
}

# Принцип: только одна модель активна
active_memory = config[active_model]["memory_gb"]  # Только одна!
```

---

## 🧪 ТЕСТИРОВАНИЕ

### **Автоматические тесты:**
```bash
# Тест принципа одного контейнера
python test_single_container_switching.py
```

**Проверяется:**
- ✅ Только один контейнер активен одновременно
- ✅ Корректное переключение между моделями
- ✅ Освобождение памяти при остановке
- ✅ Эффективность использования ресурсов

### **Ручное тестирование:**
1. Запустите приложение
2. Переключайтесь между моделями
3. Проверяйте статус через `docker ps`
4. Убедитесь, что активен только один контейнер

---

## 📊 МОНИТОРИНГ И ОТЛАДКА

### **Проверка активных контейнеров:**
```bash
# Все vLLM контейнеры
docker ps --filter "name=vllm"

# Должен быть только один в статусе "Up"
```

### **Проверка использования памяти:**
```bash
# GPU память
nvidia-smi

# Должна использоваться только одной моделью
```

### **Логи контейнера:**
```bash
# Логи активного контейнера
docker logs <container_name> --tail 20
```

### **API проверки:**
```bash
# Health check
curl http://localhost:8004/health

# Доступные модели
curl http://localhost:8004/v1/models
```

---

## 🚨 УСТРАНЕНИЕ ПРОБЛЕМ

### **❌ Модель не переключается**
**Причина:** Контейнер не может запуститься  
**Решение:**
```bash
# Проверить логи
docker logs <container_name>

# Перезапустить вручную
docker-compose -f docker-compose-vllm.yml restart <service>
```

### **❌ Несколько контейнеров активны**
**Причина:** Сбой в логике остановки  
**Решение:**
```bash
# Остановить все vLLM контейнеры
docker-compose -f docker-compose-vllm.yml down

# Запустить нужную модель
docker-compose -f docker-compose-vllm.yml up -d qwen3-vl-2b
```

### **❌ Нехватка памяти**
**Причина:** Контейнер не полностью остановился  
**Решение:**
```bash
# Принудительная очистка GPU памяти
docker system prune -f
nvidia-smi --gpu-reset
```

---

## 🔮 БУДУЩИЕ УЛУЧШЕНИЯ

1. **Предзагрузка моделей** - кеширование весов для быстрого переключения
2. **Умное планирование** - предсказание следующей нужной модели
3. **Балансировка нагрузки** - распределение запросов между репликами
4. **Мониторинг ресурсов** - автоматическая оптимизация параметров

---

**Статус:** ✅ **ПРИНЦИП РЕАЛИЗОВАН И РАБОТАЕТ**  
**Результат:** Стабильная и эффективная система управления vLLM моделями  
**Экономия памяти:** 70-80% GPU VRAM  

🎊 **Теперь можно безопасно переключаться между любыми моделями без риска нехватки памяти!**