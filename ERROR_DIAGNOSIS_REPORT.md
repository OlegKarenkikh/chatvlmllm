# 🔍 ОТЧЕТ О ДИАГНОСТИКЕ ОШИБОК

**Дата:** 25 января 2026  
**Статус приложения:** ✅ **РАБОТАЕТ КОРРЕКТНО**  
**URL:** http://localhost:8507  

---

## 📊 РЕЗУЛЬТАТЫ ДИАГНОСТИКИ

### ✅ **Все тесты пройдены успешно:**

1. **Импорты** - ✅ Все модули загружаются корректно
2. **Конфигурация** - ✅ config.yaml читается без ошибок
3. **SingleContainerManager** - ✅ Инициализируется и работает
4. **VLLMStreamlitAdapter** - ✅ Интегрирован с менеджером контейнеров
5. **UI компоненты** - ✅ Все функции рендеринга доступны
6. **Структура приложения** - ✅ Все разделы присутствуют

### 🚀 **Приложение запущено:**
- **Порт:** 8507 (8504-8506 были заняты)
- **Статус:** Работает без ошибок
- **Доступность:** HTTP 200 OK

---

## 🔍 ВОЗМОЖНЫЕ ИСТОЧНИКИ ОШИБОК

Поскольку базовые тесты прошли успешно, ошибки могут возникать в следующих случаях:

### **1. Взаимодействие с Docker контейнерами:**
```bash
# Проверка статуса vLLM контейнеров
docker ps --filter "name=vllm"

# Проверка логов контейнеров
docker logs dots-ocr-fixed --tail 20
docker logs qwen-qwen3-vl-2b-instruct-vllm --tail 20
```

### **2. Ошибки при переключении моделей:**
- Нехватка GPU памяти
- Проблемы с Docker Compose
- Сетевые проблемы (порты заняты)

### **3. Ошибки при обработке изображений:**
- Проблемы с загрузкой моделей
- CUDA ошибки
- Превышение лимитов токенов

### **4. UI ошибки:**
- Проблемы с session_state
- Ошибки рендеринга HTML/JSON
- Проблемы с BBOX визуализацией

---

## 🛠️ ИНСТРУКЦИИ ПО ДИАГНОСТИКЕ

### **Шаг 1: Проверьте логи Streamlit**
Если вы видите ошибки в браузере, проверьте терминал где запущен Streamlit:
```bash
# Логи должны показывать детали ошибок
```

### **Шаг 2: Проверьте Docker контейнеры**
```bash
# Статус всех контейнеров
docker ps -a

# Логи конкретного контейнера
docker logs <container_name> --tail 50

# Перезапуск проблемного контейнера
docker-compose -f docker-compose-vllm.yml restart <service_name>
```

### **Шаг 3: Проверьте GPU память**
```bash
# Использование GPU
nvidia-smi

# Если память заполнена, очистите
docker system prune -f
```

### **Шаг 4: Тестирование конкретных функций**
```bash
# Тест SingleContainerManager
python -c "
from single_container_manager import SingleContainerManager
manager = SingleContainerManager()
status = manager.get_system_status()
print('Активная модель:', status.get('active_model_name', 'Нет'))
"

# Тест VLLMStreamlitAdapter
python -c "
from vllm_streamlit_adapter import VLLMStreamlitAdapter
adapter = VLLMStreamlitAdapter()
print('Доступные модели:', len(adapter.available_models))
"
```

---

## 🚨 ЧАСТЫЕ ОШИБКИ И РЕШЕНИЯ

### **Ошибка: "Port already in use"**
**Решение:**
```bash
# Найти процесс на порту
netstat -ano | findstr :8507

# Завершить процесс
taskkill /PID <PID> /F

# Или использовать другой порт
streamlit run app.py --server.port 8509
```

### **Ошибка: "No module named 'single_container_manager'"**
**Решение:**
```bash
# Убедитесь, что файл существует
ls single_container_manager.py

# Проверьте Python path
python -c "import sys; print(sys.path)"
```

### **Ошибка: "CUDA out of memory"**
**Решение:**
```bash
# Остановить все vLLM контейнеры
docker-compose -f docker-compose-vllm.yml down

# Очистить GPU память
nvidia-smi --gpu-reset

# Запустить только одну модель
docker-compose -f docker-compose-vllm.yml up -d dots-ocr
```

### **Ошибка: "Connection refused" при обращении к vLLM**
**Решение:**
```bash
# Проверить, что контейнер запущен
docker ps | grep vllm

# Проверить логи контейнера
docker logs <container_name>

# Проверить доступность API
curl http://localhost:8000/health
```

---

## 📝 КАК СООБЩИТЬ ОБ ОШИБКЕ

Если вы обнаружили ошибку, предоставьте следующую информацию:

### **1. Скриншот ошибки в браузере**

### **2. Логи Streamlit из терминала**
```bash
# Скопируйте последние 50 строк из терминала где запущен streamlit
```

### **3. Статус Docker контейнеров**
```bash
docker ps -a
```

### **4. Логи проблемного контейнера**
```bash
docker logs <container_name> --tail 20
```

### **5. Использование GPU памяти**
```bash
nvidia-smi
```

### **6. Шаги для воспроизведения ошибки**
1. Какую страницу открыли
2. Какие действия выполнили
3. Когда появилась ошибка

---

## ✅ ТЕКУЩИЙ СТАТУС

**Приложение работает корректно на порту 8507**

Все основные компоненты протестированы и функционируют:
- ✅ Принцип одного активного контейнера реализован
- ✅ SingleContainerManager интегрирован
- ✅ VLLMStreamlitAdapter обновлен
- ✅ UI для управления моделями работает
- ✅ Все импорты и конфигурации корректны

**Если вы видите конкретную ошибку, пожалуйста, предоставьте детали согласно инструкциям выше.**

---

**Для получения помощи:**
1. Опишите конкретную ошибку
2. Приложите скриншот
3. Предоставьте логи
4. Укажите шаги для воспроизведения