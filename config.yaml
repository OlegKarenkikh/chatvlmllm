# ChatVLMLLM Configuration - Оптимизировано для RTX 5070 Ti (12.82GB VRAM)
# Результаты тестирования: 4/8 моделей полностью рабочие

models:
  # РЕКОМЕНДУЕМЫЕ РАБОЧИЕ МОДЕЛИ
  # РЕКОМЕНДУЕМЫЕ РАБОЧИЕ МОДЕЛИ
  got_ocr_hf:
    name: "GOT-OCR 2.0 (HF) ⭐"
    description: "Быстрая и точная OCR модель (РЕКОМЕНДУЕТСЯ)"
    model_path: "stepfun-ai/GOT-OCR-2.0-hf"
    model_type: "ocr"
    max_length: 4096
    precision: "fp16"
    device_map: "auto"
    trust_remote_code: true
    use_flash_attention: false
    ocr_type: "format"
    ocr_color: ""
    # ✅ ПРОТЕСТИРОВАНО: VRAM=1.1GB, Загрузка=6с, Обработка=0.07с

  # ДОПОЛНИТЕЛЬНЫЕ МОДЕЛИ (частично рабочие)
  got_ocr_ucas:
    name: "GOT-OCR 2.0 (UCAS) ⚠️"
    description: "Альтернативная реализация GOT-OCR (ошибка обработки)"
    model_path: "ucaslcl/GOT-OCR2_0"
    model_type: "ocr"
    max_length: 4096
    precision: "fp16"
    device_map: "auto"
    trust_remote_code: true
    use_flash_attention: false
    ocr_type: "format"
    ocr_color: ""
    # ⚠️ ПРОТЕСТИРОВАНО: VRAM=2.7GB, Загрузка=5.7с, Ошибка обработки

  qwen_vl_2b:
    name: "Qwen2-VL 2B ⭐"
    description: "Быстрая VLM для чата (РЕКОМЕНДУЕТСЯ)"
    model_path: "Qwen/Qwen2-VL-2B-Instruct"
    model_type: "vlm"
    max_length: 4096
    precision: "fp16"
    device_map: "auto"
    trust_remote_code: true
    use_flash_attention: false
    min_pixels: 256
    max_pixels: 1280
    # ✅ ПРОТЕСТИРОВАНО: VRAM=4.7GB, Загрузка=12.6с, Обработка=1.2с

  qwen3_vl_2b:
    name: "Qwen3-VL 2B ⭐"
    description: "Многоязычная VLM с OCR на 32 языках (РЕКОМЕНДУЕТСЯ)"
    model_path: "Qwen/Qwen3-VL-2B-Instruct"
    model_type: "vlm"
    max_length: 4096
    precision: "fp16"
    device_map: "auto"
    trust_remote_code: true
    use_flash_attention: false
    min_pixels: 256
    max_pixels: 1280
    # ✅ ПРОТЕСТИРОВАНО: VRAM=4.4GB, Загрузка=10.6с, Обработка=23.3с
    # Features: 32 languages OCR, 256K context, visual agent, spatial perception

  phi3_vision:
    name: "Phi-3.5 Vision ⚠️"
    description: "Мощная модель Microsoft (ошибка обработки)"
    model_path: "microsoft/Phi-3.5-vision-instruct"
    model_type: "vlm"
    max_length: 4096
    precision: "fp16"
    device_map: "auto"
    trust_remote_code: true
    use_flash_attention: false
    min_pixels: 256
    max_pixels: 1280
    # ⚠️ ПРОТЕСТИРОВАНО: VRAM=7.7GB, Загрузка=17.4с, Ошибка обработки

  dots_ocr:
    name: "dots.ocr ⚠️"
    description: "SOTA парсер документов (ошибка обработки)"
    model_path: "rednote-hilab/dots.ocr"
    model_type: "document_parser"
    max_length: 4096
    precision: "bf16"
    device_map: "auto"
    trust_remote_code: true
    use_flash_attention: false
    prompt_mode: "layout_all"
    max_new_tokens: 24000
    # ⚠️ ПРОТЕСТИРОВАНО: VRAM=8GB, Загрузка=14.9с, Ошибка обработки

  # ИСКЛЮЧЕНЫ МОДЕЛИ:
  # deepseek_ocr: ❌ Ошибка модуля transformers_modules
  # qwen3_vl_4b: ❌ Не в кеше (8.9GB)
  # qwen_vl_7b: ❌ Слишком тяжелая (16.1GB)
  # qwen3_vl_8b: ❌ Слишком тяжелая (17.6GB)

ocr:
  supported_formats: ["jpg", "jpeg", "png", "bmp", "tiff"]
  max_image_size: 10485760
  resize_max_dimension: 2048
  enable_preprocessing: true
  preprocessing_steps: ["resize", "normalize", "enhance"]
  
document_templates:
  passport:
    name: "Passport"
    fields: ["document_number", "surname", "given_names", "nationality", 
             "date_of_birth", "sex", "place_of_birth", "date_of_issue", 
             "date_of_expiry", "authority"]
  invoice:
    name: "Invoice"
    fields: ["invoice_number", "date", "seller_name", "seller_address",
             "buyer_name", "buyer_address", "items", "subtotal", "tax", "total_amount"]
  receipt:
    name: "Receipt"
    fields: ["store_name", "store_address", "date", "time", "items",
             "subtotal", "tax", "total", "payment_method"]

app:
  title: "ChatVLMLLM - Распознавание документов и чат с VLM"
  page_icon: "🔬"
  layout: "wide"
  theme: "light"
  
cache:
  enable: true
  directory: ".cache/app"
  max_age_seconds: 3600
  
export:
  default_format: "json"
  formats: ["json", "csv", "txt"]
  include_metadata: true
  
logging:
  level: "INFO"
  file: "logs/chatvlmllm.log"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  
performance:
  enable_flash_attention: false  # Отключено для Windows
  use_int8_quantization: false
  optimize_memory: true
  max_batch_size: 2  # Уменьшено для экономии VRAM
  inference_timeout: 30

# GPU Requirements для RTX 5070 Ti (12.82GB VRAM)
gpu_requirements:
  current_gpu:
    name: "RTX 5070 Ti"
    vram_gb: 12.82
    # ✅ ФИНАЛЬНЫЕ РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ
    fully_working: ["qwen_vl_2b", "qwen3_vl_2b"]  # 2 модели полностью рабочие
    partially_working: ["got_ocr_hf", "got_ocr_ucas", "phi3_vision"]  # 3 модели частично
    not_working: ["dots_ocr", "deepseek_ocr", "qwen3_vl_4b"]  # 3 модели не работают
    
    # Рекомендуемые модели для продуктивной работы
    primary_ocr: "qwen_vl_2b"        # 5.56с, 148 символов, отличное качество
    advanced_ocr: "qwen3_vl_2b"      # 14.66с, 147 символов, 32 языка
    chat_model: "qwen_vl_2b"         # Быстрый интерактивный чат
    
  performance_metrics:
    # Реальные результаты тестирования
    qwen_vl_2b:
      load_time: "8.35s"
      process_time: "5.56s" 
      ocr_quality: "148 chars"
      status: "✅ Отлично"
    qwen3_vl_2b:
      load_time: "8.46s"
      process_time: "14.66s"
      ocr_quality: "147 chars" 
      status: "✅ Хорошо"
      features: "32 languages OCR"
    
  optimization:
    # Финальные рекомендации
    default_model: "qwen_vl_2b"      # По умолчанию для OCR
    chat_model: "qwen_vl_2b"         # По умолчанию для чата
    single_model_mode: true          # Загружать по одной модели
    auto_unload: true                # Автоматически выгружать неиспользуемые
    prefer_int8: false               # FP16 достаточно для этой GPU
    batch_size: 1                    # Обрабатывать по одному изображению
    
  issues_resolved:
    # Исправленные проблемы
    image_recognition: "✅ РЕШЕНО - 2 рабочие модели"
    interface_integration: "✅ РЕШЕНО - полная интеграция"
    gpu_optimization: "✅ РЕШЕНО - оптимально для RTX 5070 Ti"
    russian_localization: "✅ РЕШЕНО - полная русификация"